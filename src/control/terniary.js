const age = 16;

console.log(
    (age >= 21)
    ? 'You can buy ciggarretes + alchohol IN US'
    : (age >= 18)
        ? 'You can buy ciggarretes + alchohol IN MX'
        : 'YOU CANNOT BUY ONLY 18+'
);
